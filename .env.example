# Database Configuration
# Update this with your actual database credentials
DB_URL=postgresql+asyncpg://username:password@host:port/database_name

# Application Configuration - update as needed
APP_NAME=bankAPI
APP_VERSION=0.1.0
DEBUG=True

# JWT Authentication Configuration
# IMPORTANT: Change SECRET_KEY in production! Generate with: openssl rand -hex 32
SECRET_KEY=update_this_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=30